# 验收启动说明

## 当前包状态

- 包状态：`optimized acceptance-ready candidate / evidence included`
- 上一已签收基线包：`livestock-monitor-user-acceptance-package-20260707-0041.zip`
- 本优化候选包不得覆盖上一基线包。

## 端口和服务

- MySQL: `127.0.0.1:3306`
- 平台后端: `http://127.0.0.1:8000`
- TCP/HJ212 接收: `127.0.0.1:9100`
- Web 前端: `http://127.0.0.1:5173`
- 验收 ACK: PC 端必须收到并校验合法 HJ212 `CN=9014`
- PC 端 ACK 等待默认值: `3.0s`

## 验收前必须做

1. 重启平台 backend 和 TCP 接收服务，确保新接收时间逻辑生效。
2. 使用新 PC 上传数据做验收依据，不使用 seed/demo 数据作为真实验收依据。
3. 历史 8 小时时间错误不回填，本次只验证重启后新接收数据。
4. 短信仍为 `dry-run/noop` 预留能力，不代表真实短信服务商已接入。

## 启动和守门命令

环境检查：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\check-env.ps1
```

端口检查：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\check-ports.ps1
```

启动平台：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\start-all.ps1
```

启动平台并尝试启动 PC 客户端：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\start-all.ps1 -StartPcClient
```

服务已启动时，只跑真实 TCP 验收守门：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\check-acceptance.ps1 -SkipStartup
```

通过判据：

- `status = passed`
- `endpoint.host = 127.0.0.1`
- `endpoint.port = 9100`
- `ackValidation.valid = true`
- `ackValidation.cn = 9014`
- `protocolLog.status = accepted`

## 优化证据清单

PC 优化证据：

- `pc-client/docs/qa_pc_optimization_home_trend_current_20260707.png`
- `pc-client/docs/qa_pc_optimization_interval_dropdown_20260707.png`
- `pc-client/docs/qa_pc_optimization_analysis_current_20260707.png`
- `pc-client/docs/qa_pc_optimization_time_alignment_20260707.txt`
- `pc-client/docs/qa_pc_optimization_responsiveness_20260707.txt`

平台证据：

- `logs/acceptance/tcp-acceptance-20260707-132313.json`
- `logs/acceptance/tcp-acceptance-20260707-124843.json`
- `logs/acceptance/tcp-acceptance-20260707-124848.json`
- `docs/product/progress-log.md`
- `ACCEPTANCE_EVIDENCE_MANIFEST.md`

平台 12:49 时间修复摘要记录在 manifest 和进度日志中：

- `DataTime=2026-07-07T12:49:34+08:00`
- `receivedAtBeijing=2026-07-07T12:49:35+08:00`
- `storedAtBeijing=2026-07-07T12:49:35+08:00`
- `/data/analysis` 已记录 `dataSource=database`

## 日志和失败收集入口

验收失败时收集：

- `VERSION.txt`
- `ACCEPTANCE_EVIDENCE_MANIFEST.md`
- `logs/check/`
- `logs/mysql/`
- `logs/backend/`
- `logs/tcp/`
- `logs/web/`
- `logs/acceptance/`
- `logs/pc-client/`
- `pc-client/docs/`
- 执行 `check-acceptance.ps1` 时的终端截图或输出

优先定位：

- 连接失败：看 `check-ports.ps1` 输出和 `logs/tcp/`
- 无合法 `CN=9014` ACK：看 `logs/tcp/`、`logs/acceptance/`、PC 日志
- 页面无真实数据：确认是否使用新 PC 上传数据，避免 seed/demo 数据误导
- 时间仍差 8 小时：确认 backend/TCP 已重启，并只检查新接收数据

## PC 客户端启动入口

`start-all.ps1 -StartPcClient` 会按顺序检查：

- `pc-client\start.ps1`
- `pc\start.ps1`
- `pc-client\start.bat`
- `pc\start.bat`

找不到入口时会提示 PC 客户端未入包或启动入口缺失。
